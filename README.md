# Operating Systems Lab

**Institute of Engineering and Management**
**Operating Systems Lab (BCACC 392)**

Solutions and terminal-command writeups for the OS Lab assignments — each assignment folder contains a `README.md` with the command, an explanation, and the output (with terminal screenshots where available).

**Name:** Arnab De | **Roll No.:** 63 | **Section:** A | **Year:** 2nd

## Contents

| Assignment | Topics Covered |
|---|---|
| [Assignment 1](./Assignment_1/README.md) | Basic Linux commands — `date`, `cal`, `who`/`whoami`, `pwd`, `mkdir`, `cd`, file creation & display (`cat`), `file`, `cp`, `wc`, `head`, `tail`, `free` |
| [Assignment 2](./Assignment_2/README.md) | File permissions (`ls -l`, `chmod`), sorting (`sort`), field extraction (`cut`), deduplication (`uniq`), and text filtering (`grep`) |

## Structure

Each assignment folder follows the same pattern:

```
Assignment_N/
├── README.md          # commands, explanations, outputs
└── screenshots/        # terminal screenshots backing each answer (where available)
```

## Requirements

- Any Linux/Unix terminal (Bash)
